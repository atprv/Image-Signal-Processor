# Dataset Files

Large HDF5 datasets are not included in this clean export. Generate them with `scripts/extract_patches.py`, `scripts/precompute_isp_outputs.py`, or upload the compact Colab datasets described in the notebooks.
